# 用户端雨景与毛玻璃视觉实施规划

## 1. 目标

将登录后用户端页面的背景和容器材质统一为首页现有的“雨景 + 毛玻璃”视觉，同时完整保留当前项目的业务实现。目标页面包括 `/dashboard`、`/keys`、`/usage`、`/monitor`、`/subscriptions`、`/purchase`、`/orders`，以及所有使用统一用户端 `AppLayout` 的登录后页面。

本批只改变显示层：背景图、雨幕、玻璃透明度、模糊、边框和阴影。页面内容、文案来源、配置读取、API、请求参数、路由、权限、登录状态、功能开关、状态管理、按钮事件和数据写入均保持现状。

## 2. 范围边界

纳入：

- `AppLayout` 承载的 `requiresAuth: true` 且非管理端路由；管理员访问个人用户路由时也使用用户端材质。
- 用户端公共卡片、统计卡片、图表面板、表格容器、监控卡片和页面内的主要面板。
- 用户端侧栏和顶部栏的背景材质，使其与页面雨景保持统一。

暂不纳入：

- 登录、注册、找回密码、公开查询和支付回调/外部支付弹窗页面。
- `/admin/**` 管理端页面。
- 外部 iframe、自定义 HTML 内部内容和代码编辑器内部的专用背景。
- 按钮、输入框、文字、图标颜色、布局尺寸、圆角、间距和业务状态样式，除非覆盖背景是维持可读性所必需的纯材质调整。

## 3. 设计原则

1. **作用域隔离**：在 `AppLayout` 根节点增加用户端标记（`user-glass-surface`），所有覆盖选择器必须从该作用域开始，禁止重写全局 `body`、全局 `.card` 或管理端样式。
2. **复用首页资产**：直接复用 `RainyBackground.vue` 的三张雨景图、雨幕 Canvas、玻璃水滴 Canvas 和深夜叠层；不复制一套新的背景实现。
3. **只提取纯材质**：不把 `GlassPane.vue` 的 `min-height`、居中、滚动、`select-none` 等布局行为带入业务页，只提取背景、模糊、边框和阴影。
4. **保留页面结构**：公共 `.card` 通过作用域 CSS 统一材质；绕过公共类的容器只补一个语义 class，避免重写模板和业务逻辑。
5. **主题可读性**：深色主题使用首页深夜玻璃参数；浅色主题保留现有深色文字，通过较亮的半透明卡片和背景洗色层保证对比度。
6. **层级稳定**：雨图/雨丝背景为 `z-index: 0`，洗色层为 `z-index: 1`，Teleport 到 `body` 的水滴 canvas 为 `z-index: 9`，页面内容为 `z-index: 10`，顶部栏/侧栏沿用现有 `z-index`，弹窗和 Toast 沿用现有更高层级；用户端根节点不建立会压住 Teleport 水滴的独立 stacking context。

## 4. 分阶段实施

### 阶段 A：基线和规划

- 确认当前分支干净、首页雨景组件可复用、用户端路由和 `AppLayout` 入口完整。
- 建立本规划文档，记录范围、风险、验收和后续合并规则。

### 阶段 B：布局作用域和背景

- 在 `AppLayout.vue` 中按路由元数据计算 `isUserSurface`：`requiresAuth === true` 且 `requiresAdmin !== true`。
- 仅在 `isUserSurface` 时挂载 `RainyBackground`，隐藏用户端原有不透明灰色背景和 mesh 装饰。
- 增加浅色/深色主题洗色层，并给内容容器明确的相对层级。
- 继续使用现有站点配置、侧栏、顶部栏和页面 slot，不改变导航和生命周期业务逻辑。

### 阶段 C：共享材质样式

- 新建独立 `user-glass-surface.css`，定义用户端 CSS 变量和作用域选择器。
- 覆盖 `.card`、`.glass-card`、`.card-glass`、`.stat-card`、`.table-container` 等已有公共容器的背景、边框、模糊和阴影。
- 覆盖用户端侧栏、顶部栏及常见 tabs/dropdown 的背景材质；保留尺寸、位置和交互状态。
- 让新文件在 `subnexus-legacy-surface.css` 之后加载，以便只在材质属性上统一旧版页面。

### 阶段 D：绕过公共类的页面接入

- 为订阅卡、支付二维码、监控 V1/V2/V3、Batch Image 主面板、表格布局容器等明确写死不透明背景的容器补充语义 class。
- 不修改这些页面的请求、计算、事件、路由或权限代码。
- 保留现有特殊卡片（例如活动卡）的尺寸和圆角覆盖，只让其获得统一的玻璃材质。

### 阶段 E：验证

- 静态：`git diff --check`、前端 typecheck、定向组件测试、完整 Vitest、生产构建。
- 路由：至少检查 `/dashboard`、`/keys`、`/usage`、`/monitor`（V1/V2/V3）、`/subscriptions`、`/purchase`、`/orders`，并确认 `/admin/dashboard` 仍使用原背景。
- 视觉：桌面和移动尺寸、浅色和深色主题、加载/空状态、图表、表格横向滚动、下拉框、弹窗和侧栏层级。
- 行为：点击导航、按钮、主题切换、语言切换和现有 API 请求路径不发生变化。
- 性能：确认每个页面只挂载一组背景 Canvas；页面卸载后监听器和动画按组件现有生命周期释放。

## 5. 风险与处理

- **上游合并冲突**：集中在 `AppLayout.vue` 和少数 class 标记；独立 CSS 文件尽量不与业务文件重叠。合并时优先保留上游逻辑，再恢复用户端作用域标记。
- **旧版 CSS 优先级**：新文件在 legacy 文件之后加载，并使用明确的用户端作用域；不删除或重写 legacy 文件。
- **浅色主题对比度**：通过洗色层和主题变量调整透明度，不强制把用户端文字改为白色。
- **Canvas 层级**：背景和水滴均不接收指针；用户端通过背景组件可选层级把水滴固定在洗色层与主内容之间，导航和弹窗继续使用现有更高层级。`prefers-reduced-motion` 或页面不可见时只绘制静态帧并释放动画监听器。
- **直接背景绕过**：仅对盘点出的语义容器补 class，不使用全局通配符，避免误伤按钮、输入框和代码块。

## 6. 交付和后续合并规则

- 所有本批修改保持为独立 UI 提交，建议提交顺序为：规划文档、共享布局/样式、绕过点标记、测试/记忆记录。
- 不修改依赖锁文件、后端、数据库、路由守卫或生产配置。
- 上游更新前先同步上游分支，再在本分支解决布局文件的局部冲突；完成 typecheck、构建和用户端冒烟后再合并。
- 本批发布必须建立新的生产回滚目标。`prepare` 先完整核验历史旧 SubNexus anchor，并只把当前 live 的完整 ID、`.Image`、`.Config.Image`、唯一目标名称和运行状态写入 manifest，不改变 Docker 状态；维护者执行最终 `switch` 时，wrapper 停止并把该 live 重命名为 `production-app-ui-prior-<run-id>`，以 stopped 容器形式保留，随后才创建并启动候选。
- 本轮 `rollback` 只恢复上述新保留的切换前 live，不再恢复更早的历史 SubNexus。现行 wrapper 仍把历史 anchor 作为贯穿 switch/rollback 窗口的连续性门禁，因此该 anchor 及其容器/镜像不得删除；空间不足时只能在逐项核实身份并保存清理审计后，精确删除其他已失效 run 备份或无引用垃圾。不得使用 `docker system prune`、`docker volume prune` 或模糊匹配清理，本轮新的回滚目标不得纳入任何清理。

## 7. 验收标准

- 用户端登录后页面显示首页同源雨景和玻璃材质，内容和功能与改动前一致。
- 管理端、认证页、公开页和外部支付流程不被该作用域改变。
- 所有既有测试和构建通过，无横向溢出、不可点击控件、文字遮挡或弹窗层级错误。
- `git diff` 能清楚证明改动仅限用户端视觉层和本规划/记忆文档。

## 8. 当前实施状态（2026-09-08）

- 用户端背景、作用域玻璃材质和绕过公共类的语义标记已实施；管理端、认证页、公开页和外部支付流程继续隔离。
- 审核发现的四项问题已修复：Teleport 水滴层级可见；快捷操作和最近用量 hover 保留；DataTable sticky 表头/列使用不透明主题底色；订单表移除额外裁切包裹并恢复原结构。
- 桌面/移动与明暗主题视觉冒烟通过；Vitest `294/294` 文件、`2041/2041` 测试、typecheck、lint、production build 和 `git diff --check` 通过。
- 线上发布前置仍在进行；不可变候选 commit=`187b128bd32d1e06ad6e08817632e7c6b5ccca92`、tree=`e81b71b7f136da0a62bd51e266f041c6312e6b0b` 已固定并推送。镜像、Gate、备份、prepare run 和最终审计按实际结果回填到切换手册第 15 节及迁移台账，最终 switch 由维护者手动执行。
